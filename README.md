☁️ คู่มือการ Deploy Thai Sentiment Insights (Render.com)
1. เตรียมความพร้อมของโครงสร้างไฟล์ (Local)

ก่อนจะ Push ขึ้น GitHub ต้องมั่นใจว่าโครงสร้างเป็นแบบนี้เพื่อให้คำสั่งที่พี่ให้ไปทำงานได้ถูกต้อง:

Plaintext
/root-folder
  ├── backend/
  │    ├── main.py
  │    ├── requirements.txt
  │    └── model/
  │         └── sentiment_model_v1.joblib
  ├── frontend/
  │    ├── package.json
  │    ├── src/
  │    └── ... (ไฟล์ React อื่นๆ)
  └── .gitignore
2. การตั้งค่าบนหน้าเว็บ Render.com

เมื่อแอ๋มสร้าง New > Web Service และเชื่อมต่อ GitHub แล้ว ให้ตั้งค่าตามนี้:

Runtime: Python 3

Build Command:

Bash
pip install -r backend/requirements.txt && cd frontend && npm install && npm run build
Start Command:

Bash
uvicorn backend.main:app --host 0.0.0.0 --port $PORT
3. การตั้งค่า Environment Variables (สำคัญมาก)

ไปที่เมนู Environment แล้วกด Add Environment Variable เพื่อคุมเวอร์ชันให้ตรงกับโมเดลที่เราเทรนมา:

Key	Value	เหตุผล
PYTHON_VERSION	3.11.8	เพื่อให้ Scikit-learn ทำงานได้เสถียรตามที่เราเทรน
NODE_VERSION	20.x	เพื่อให้ Build Frontend (React) ผ่านฉลุย
4. จุดที่ต้องระวังใน Code (Checklist)

ก่อนจะกด Deploy รอบสุดท้าย เช็ค 2 จุดนี้ใน backend/main.py :

การดึงหน้าเว็บ (Static Files):

Python
# ต้องชี้ไปที่โฟลเดอร์ dist ที่เราสั่ง build มา
app.mount("/", StaticFiles(directory="frontend/dist", html=True), name="static")
เวอร์ชันของ Scikit-learn:

ใน backend/requirements.txt ต้องระบุเวอร์ชันให้ตรงกับที่ใช้เซฟโมเดล

แนะนำ: scikit-learn==1.4.2

5. วิธีการตรวจสอบผลงาน

เมื่อกด Deploy แล้ว ให้สังเกตที่หน้า Logs:

Build Successful 🎉: แสดงว่าติดตั้ง Library และ Build React สำเร็จ

Model loaded successfully!: แสดงว่าเวอร์ชัน Python และ Scikit-learn ตรงกันจนโหลดโมเดลได้

Your service is live 🎉: กดลิงก์ .onrender.com เข้าไปทดสอบได้เลย!
